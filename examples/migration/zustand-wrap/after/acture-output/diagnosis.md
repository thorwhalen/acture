# Diagnosis — zustand-wrap

**Generated by:** `migration-diagnose` against `examples/migration/zustand-wrap/before/`
**State library:** zustand (vanilla)
**Stores found:** 1 (`src/store.ts` — notes)
**Total candidates:** 8
**Priority ≥ 4:** 5

## Stores

| Store | Actions | File |
| --- | --- | --- |
| notes | `addNote`, `removeNote`, `toggleDone`, `setBody`, `setDueDate`, `archiveDone`, `setTheme`, `setFontSize` | `src/store.ts` |

## Top candidates

| # | Proposed id | Location | Params | Complexity | Priority |
| --- | --- | --- | --- | --- | --- |
| 1 | `app.note.add` | `src/store.ts` (addNote) | `{ title: string }` | moderate | 5 |
| 2 | `app.note.toggleDone` | `src/store.ts` (toggleDone) | `{ id: string }` | simple | 5 |
| 3 | `app.note.remove` | `src/store.ts` (removeNote) | `{ id: string }` | simple | 4 |
| 4 | `app.note.setBody` | `src/store.ts` (setBody) | `{ id: string, body: string }` | moderate | 4 |
| 5 | `app.note.setDueDate` | `src/store.ts` (setDueDate) | `{ id: string, date: string }` | moderate | 4 |
| 6 | `app.note.archiveDone` | `src/store.ts` (archiveDone) | — | simple | 3 |
| 7 | `app.settings.setTheme` | `src/store.ts` (setTheme) | `{ theme: enum }` | simple | 3 |
| 8 | `app.settings.setFontSize` | `src/store.ts` (setFontSize) | `{ size: number }` | simple | 3 |

## Skipped

- `useState(title)` in `NewNoteForm` — local UI state, not a candidate.
- The `<input type="checkbox">` `onChange` in `NotesList` calls `toggleDone` — that store action IS captured as candidate #2; the onChange itself is not a separate candidate.
