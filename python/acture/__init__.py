"""acture — placeholder Python distribution.

The acture library is a TypeScript/JavaScript package published on npm
(https://www.npmjs.com/package/acture). This PyPI distribution exists to
reserve the `acture` name on PyPI for a future Python companion package
(currently post-v1 per https://github.com/thorwhalen/acture/blob/main/docs/v1_plan.md).

Installing this package gives you nothing functional today. If you are
looking for the acture command-dispatch library, install it from npm:

    npm install acture
    # or
    pnpm add acture

See the GitHub README for the full ecosystem.
"""

__version__ = "0.2.0"
"""Tracks the npm package version exactly. Auto-synced by
`scripts/sync-python-version.mjs` on release."""
